<%= render "shared/breadcrumbs", current_page: :new_admins_flight  %>

<div class="card shadow rounded">
  <h4 class="card-header bg-primary bg-gradient text-white">
    New flight
  </h4>
  <div class="card-body">
    <%= render "form", flight: @flight %>
  </div>
</div>


<%= form_for([:admins, flight]) do |form| %>
  <% if flight.errors.any? %>
    <div class="text-danger">
      <p><%= pluralize(flight.errors.count, "error") %> prohibited this flight from being saved:</p>

      <ul>
        <% flight.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <%= form.label :name, class: 'form-label' %>
    <%= form.text_field :name, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :aeroplane_id, class: 'form-label' %>
    <%= form.number_field :aeroplane_id, class: 'form-control' %>
  </div>

  <div class="mb-3">
    <%= form.label :departure_date, class: 'form-label' %>
    <%= form.datetime_field :departure_date, class: 'form-control' %>
  </div>

  <%= form.submit "Create", class: "btn btn-primary" %>
<% end %>
